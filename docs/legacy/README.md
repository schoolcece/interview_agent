# Legacy 文档归档

本目录下所有文件均为 **v0 阶段的历史遗留文档**，描述的架构、数据模型、实现状态与当前项目**严重不一致**。

归档原因：
- 描述 PostgreSQL + RLS + org_id 多租户架构 —— 现已改为 MySQL + C 端无租户
- 列出大量 Entity / Service / Repository 实际从未实现
- 部分字段、API 端点、配置项已被移除或重命名

**不要将本目录内容作为开发参考。**
请以上级 `docs/ARCHITECTURE.md` 与 `docs/AGENT_DESIGN.md` 为准。

保留期建议：2 周后可整体删除。
